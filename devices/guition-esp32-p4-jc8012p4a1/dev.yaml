# =============================================================================
# DEV CONFIG - Local development entry point (10" P4 panel)
# =============================================================================
# Flash with: esphome run dev.yaml
# =============================================================================

substitutions:
  name: "espcontrol-10inch-p4"
  friendly_name: "EspControl 10inch P4"
  espcontrol_component_source: "../../components"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  espcontrol: !include packages.yaml
